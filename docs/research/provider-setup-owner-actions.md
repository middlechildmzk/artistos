# Provider setup owner actions

Provider accounts and secrets must be created by the ArtistOS owner. The application should present precise setup steps and validate configuration without exposing secrets.
