# Connector boundaries

Credentials are configured by the account owner. ArtistOS can implement, validate and expose connector readiness, but cannot invent provider keys or accept provider terms on the user's behalf.
